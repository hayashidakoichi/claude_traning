# Claude Code 中級者研修 for Mac

サブタイトル：　チャットを卒業してAIエージェントとして利用する！

![Claude Code](https://cdn.prod.website-files.com/6889473510b50328dbb70ae6/68c469d41149ace562bfd24d_og-claude-product-claude-code.jpg)
*出典: [Anthropic — Claude Code](https://claude.com/product/claude-code)*

---

## はじめに

この研修は、普段 Claude を **チャット（Web 版やデスクトップ版）** で使っている方が、**Claude Code（AI エージェント）** に乗り換えて、業務そのものを自動化・仕組み化できるようになることを目指します。

### この研修の前提

| 項目 | 内容 |
|---|---|
| 受講者 | 普段は Claude をチャットで使っている非エンジニアの方 |
| 環境 | 全員 Mac（macOS）／会社の Google Workspace を利用 |
| プラン | **すでに Claude の有料プラン（Pro / Max / Team など）に加入済み**（Claude Code は有料プラン専用。招待メールの承認などは不要） |
| 形式 | ハンズオン（実際に手を動かしながら進めます） |
| 時間 | 全 2 回・各 3 時間（合計 6 時間） |

> 専門用語には必ず日本語の補足を添えます。ターミナルやコマンド操作が初めてでも進められるように作っています。

### この研修の進め方 — 3 つの質問に答える形で進みます

本研修は、受講者のみなさんから実際に寄せられた 3 つの質問（QA1〜QA3）を学習の軸にしています。プライバシーに配慮して、質問者はイニシャルで表記します。

| | 質問者 | 悩み | この研修での答え |
|---|---|---|---|
| QA1 | Ts さん | 初めて使うツールの設定でつまずく。開発より「業務の自動化」を身につけたい | Mac 環境で正しく Claude Code を使えば、PC 環境起因のつまずきは消える |
| QA2 | M さん | 会話が混ざる／時系列を理解しない。ためたデータを学習・活用させられない | **記憶機能（メモリ）** で「覚えさせる」仕組みを作る |
| QA3 | O さん | 毎回ゼロからチャットで指示している。定型業務を一連の流れで自動化したい | **Skills × MCP × スケジュール** で業務を仕組み化する |

### チャットと Claude Code の違い（この研修のいちばん大事な考え方）

チャットは「**質問するたびに、ゼロから指示する**」道具です。便利ですが、同じ作業を毎回イチから頼み直すことになります。

Claude Code は「**作業者（エージェント）として動く**」道具です。一度教えた手順を覚えさせ、外部サービス（メール・カレンダー・課題管理）にもつなぎ、決まった時刻に自動で動かすこともできます。

```
  チャット                       Claude Code（エージェント）
  ┌────────────┐                ┌──────────────────────────┐
  │ 質問 → 回答 │                │ 指示 → AI が自分で          │
  │ 毎回ゼロから │       →        │ ファイル操作・コマンド実行・ │
  │ やり直し    │                │ 外部サービス連携まで遂行     │
  └────────────┘                └──────────────────────────┘
```

「チャットを卒業する」とは、**この“作業者として動く”使い方に移ること**です。

### 全 2 回の全体像

| 回 | テーマ | 主な内容 | 対応する質問 |
|---|---|---|---|
| **第 1 回（3 時間）** | チャットを卒業する | 座学（なぜエージェントか）／Mac セットアップ／記憶機能 | QA1・QA2 |
| **第 2 回（3 時間）** | 業務を仕組み化する | Skills × MCP × スケジュールで定型業務を自動化 | QA3 |

---

# 第 1 回（3 時間）：チャットを卒業する

> ゴール：自分の Mac で Claude Code を正しく起動でき、記憶機能で「覚えさせる」ところまで体験する。

| パート | 時間の目安 | 内容 |
|---|---|---|
| Part 1 | 約 45 分 | 座学：なぜ Claude Code（エージェント）なのか |
| Part 2 | 約 70 分 | Claude Code for Mac セットアップ |
| 休憩 | 10 分 | |
| Part 3 | 約 60 分 | 記憶を持たせる（QA2：メモリ機能） |

---

## Part 1　座学：なぜ Claude Code（エージェント）なのか（約 45 分）

### 1-1. Claude とは — なぜ今 Claude なのか

Claude は Anthropic（アンソロピック）社が開発している AI です。AI アシスタントは数多くありますが、Claude は **正確性・安全性・コーディング能力** で急速に支持を広げています。

| | ChatGPT（OpenAI） | Gemini（Google） | Claude（Anthropic） |
|---|---|---|---|
| 得意分野 | 幅広い汎用タスク | Google サービスとの統合・検索連動 | 長文理解・正確な推論・コーディング |
| 特徴 | 最も早く普及した先駆者 | Google 検索・Workspace と一体化 | 安全性と正確性を重視した設計 |

> **ポイント**：ChatGPT や Gemini が「使えない」わけではありません。普段の調べ物には ChatGPT、業務の自動化には Claude、というように使い分けるのも一つの手です。ただし「正確さ」と「業務を任せる」文脈では、Claude が現時点で最も高い評価を得ています。

### 1-2. Claude の 4 つのインターフェース — どれを覚えればいい？

Claude には使い方の異なる 4 つの入り口があります。**結論から言うと、本研修で使う「Claude Code（CLI）」だけ覚えておけば大丈夫**です。理由は、Claude Code が最も自由度が高く、他の 3 つでできることはすべて Claude Code でもできるからです（逆はできません）。

```
    自由度・できることの幅
    ▲
    │  ★ Claude Code（CLI）   ← 本研修で使う。これだけ覚えればOK
    │    AI が自律的に作業を遂行する
    │    ファイル操作・コマンド実行・外部サービス連携
    │
    │  ● Cowork（デスクトップ版の拡張機能）
    │    ファイル読み書き・マルチステップ実行
    │
    │  ■ デスクトップ版
    │    ショートカットで即呼び出し
    │
    │  ▼ Web 版
    │    ブラウザだけ・チャット形式の質問応答
    │
    └──────────────────────────► 手軽さ
```

| 比較項目 | Web 版 | デスクトップ版 | Cowork | Claude Code（CLI） |
|---|---|---|---|---|
| 利用方法 | ブラウザで claude.ai | 専用アプリ | デスクトップ版内の機能 | ターミナルで `claude` を実行 |
| 基本動作 | 1 問 1 答 | チャット＋呼出 | 指示を出して放置 → 完成物 | AI が自律的に複数ステップを遂行 |
| ファイル操作 | アップロード/ダウンロードのみ | 同左 | ローカル読み書き可 | ローカル読み書き可 |
| コマンド実行 | 不可 | 不可 | 限定的 | あらゆるコマンドを実行可能 |
| 外部ツール連携 | プラグイン | MCP | MCP＋プラグイン | MCP 連携 |
| 向いている用途 | 質問・相談 | 日常の AI 活用 | 資料作成・データ整理 | 開発・**業務自動化・業務フロー構築** |

> **「CLI」とは？** Command Line Interface の略で、**ターミナル（黒い画面）から文字で操作する**方式のこと。マウスでクリックする GUI と対になる言葉です。最初は黒い画面に戸惑いますが、やることは「日本語で指示を打つ」だけです。

### 1-3. 「AI モデル」と「AI エージェント」の違い

ニュースで見る **Claude Opus** や **Claude Sonnet** は「AI モデル」＝**頭脳**にあたる部分です。一方 **Claude Code** は、その頭脳に**手足を与えた AI エージェント**です。

| | AI モデル（Claude Opus / Sonnet 等） | AI エージェント（Claude Code） |
|---|---|---|
| 役割 | 質問に答える「頭脳」 | 頭脳を使って作業を遂行する「作業者」 |
| できること | テキストの入出力のみ | ファイル操作・コマンド実行・外部連携まで |
| たとえ | 何でも知っている相談役 | 指示どおりに手を動かすアシスタント |

チャット（＝賢い相談役）から、Claude Code（＝実際に手を動かす作業者）へ。これが「チャットを卒業する」ということです。

### 1-4. なぜチャット AI ではなくエージェント型 AI なのか

| 比較項目 | 従来のチャット AI | Claude Code |
|---|---|---|
| 動作モデル | 質問に回答する | AI が作業者として動く |
| ファイル操作 | 不可 | ローカルファイルの読み書き可能 |
| コマンド実行 | 不可 | ターミナルコマンドを実行可能 |
| 作業の自律性 | 1 問 1 答 | 複数ステップを自動で遂行 |

**Claude Code（CLI）が最も強力な 5 つの理由**

1. **ファイルを直接操作できる** — アップロード・ダウンロードの手間なく、PC 上のファイルをそのまま読み書きする
2. **コマンドを実行できる** — データ加工・ファイル変換など、ターミナルでできることをすべて任せられる
3. **やり直しがきく** — 変更の履歴を残せるため、失敗しても安全に元へ戻せる
4. **複数の作業を同時に走らせられる** — 1 つを実行中に別の作業を始められる
5. **スキルとして再利用できる** — 一度作ったワークフローを保存し、コマンド 1 つで再実行できる（← 第 2 回のテーマ）

> **「手動の方が早い」と感じたときに思い出すこと**：初回は試行錯誤で時間がかかりますが、一度うまくいった手順は **スキル（Skills）** として保存でき、次回からは一瞬です。さらに AI が作業している 10 分間、自分は別の作業ができます。手動は毎回同じ時間がかかりますが、Claude Code は**回を重ねるほど速くなります**。

### 1-5. Claude Code のセキュリティとデータプライバシー

![Claude Code Security](https://cdn.prod.website-files.com/68a44d4040f98a4adf2207b6/69050f2159f55e8c6f5f8e0f_og-claude-generic.jpg)
*出典: [Anthropic — Claude Code Security](https://www.anthropic.com/news/claude-code-security)*

業務で AI を使うとき、最も気になるのは「入力した情報が学習に使われないか」「社内データが外部に漏れないか」でしょう。Claude Code はこの点で、他の AI ツールより明確な保証を提供しています。

- **入力データは AI の学習に一切使われない**（Anthropic の利用規約で保証。デフォルトでそうなっています）
- **ファイルは自分の PC 上で処理される**（Claude Code はローカルで動作。クラウドに勝手にアップロードされません）
- **権限を細かく制御できる**（どのコマンドを実行してよいか／だめか、を設定できる → 付録 B）

| セキュリティ項目 | Claude（Anthropic） | ChatGPT（OpenAI） | Gemini（Google） |
|---|---|---|---|
| データの学習利用 | **しない**（デフォルト） | オプトアウトが必要 | 無料版は利用、有料版は除外 |
| ローカル実行 | **PC 上で動作** | クラウド処理 | クラウド処理 |
| 権限制御 | ファイル・コマンド単位で許可/拒否 | なし | なし |

> **「AI に社内データを読ませて大丈夫？」と聞かれたら**：(1) 入力データは学習に使われない、(2) ファイルは PC 上で処理されクラウドに上がらない、(3) 読み取り・実行の権限を細かく制御できる、の 3 点を伝えましょう。

### 1-6. 利用する 2 つのツール — Claude Code と VS Code

本研修で使う道具は **2 つだけ**です。役割を押さえておきましょう。

| ツール | 何をするもの？ | たとえると… |
|---|---|---|
| **Claude Code** | AI が自律的にファイル作成・編集・コマンド実行を行うエージェント | 指示どおりに手を動かすアシスタント（作業者） |
| **VS Code** | Microsoft 製の無料エディタ。ファイルの中身を整形して見られる | Claude Code の作業を**確認するための窓**（ビューア） |

**なぜ VS Code が必要なのか？**

Claude Code はターミナル（黒い画面）で操作します。一方、Claude Code が作ったファイル（議事録・提案書・比較表など）の**中身を整形して確認・プレビュー**するには、ターミナルよりも VS Code のほうが圧倒的に見やすいのです。

- Claude Code が作った Markdown ファイルを、**見出しや表が整った状態でプレビュー**できる
- 左にファイル一覧、右にプレビューを並べて、**指示 → 確認 → 修正依頼**をテンポよく回せる

> **Git について**：Claude Code は変更履歴を残して「やり直し」をきかせる仕組み（Git）も使えますが、本研修では深入りしません。「失敗しても元に戻せる安心の仕組みがある」程度に覚えておけば十分です。

**いちばん大事な考え方：フォルダ単位で運用する**

Claude Code は、**起動したフォルダ（`pwd` で表示される場所）を起点（作業ディレクトリ）**にして動き、基本的にはこの配下のファイルを読み書きします。ただし「フォルダの外には絶対に触れない」と**保証されているわけではありません**。実際にどこまで操作できるかは、許可・拒否の**権限設定**で決まります（→ 付録 B の `settings.json`、確認は `/permissions`）。設定や承認しだいでは、起動フォルダの外のファイルやコマンドにも及びえます。

```
~/Desktop/claude-training/  ← ここで Claude Code を起動すると…
├── 議事メモ.md             ← この中のファイルが主な作業対象
├── 提案書.md
└── ...
```

➡ だからこそ、**業務の本番ファイルがある場所で直接起動せず、研修・作業用のフォルダを 1 つ作って、その中で動かす**のが鉄則です。これを「フォルダ単位の運用」と呼びます。あわせて、致命的な操作は権限設定であらかじめブロックしておくと安全です（→ 付録 B）。第 2 回の自動化でも、この考え方が土台になります。

### 1-7. QA1（Ts さん）への答え — つまずきは Mac 環境で消える

> **質問**：初めて使うツールの設定が感覚的に分からず、スクショを貼って言葉で質問して無理やり解決している。開発より、あらゆる業務を自動化するスキルを身につけたい。

**答え**：

1. **スクショ＋言葉で聞くやり方は、そのまま正解です。** 迷ったら情報を与えて聞きまくれば、ほぼ解決します。エラーが出ても、画面をそのまま貼って「どうすればいいですか？」と聞き、提示された解決策に「お願いします」と返すだけで、Claude Code が原因の特定から修正まで自動でやってくれます。
2. **環境起因のつまずきは、Mac で正しく設定すれば消えます。** これまでの「設定が分からない」「うまく動かない」の多くは、PC 環境（Windows やデスクトップ版など）の差異が原因でした。本研修のとおりに **Claude Code for Mac** を設定すれば、その問題はほぼ存在しません。
3. **本研修のゴールは、まさに「開発ではなく業務の自動化」です。** 第 2 回で、議事録から会議設定・メール下書きまでの一連の流れを自動化します。

> **エラーが出ても大丈夫**：赤いエラーが出ても慌てないでください。`①そのまま「どうすればいいですか？」と聞く → ②「お願いします」と返す` の 2 ステップで、たいてい解決します。

---

## Part 2　Claude Code for Mac セットアップ（約 70 分）

ここからは実際に手を動かします。

### 2-1. ターミナルを起動する（2 つの起動方法）

Claude Code は **ターミナル** から操作します。ターミナルは、文字でコマンドを打ち込む「黒い画面」のことです。Mac では 2 つの場所からターミナルを開けます。**両方とも開けるようにしておきましょう。**

**方法 A：Mac 標準の「ターミナル.app」**

1. `Cmd + Space` で Spotlight 検索を開く
2. 「ターミナル」と入力 → Enter

（または Finder →「アプリケーション」→「ユーティリティ」→「ターミナル.app」）

**方法 B：VS Code の中のターミナル**（VS Code セットアップ後に使えます）

- VS Code のメニュー →「ターミナル」→「新しいターミナル」
- ファイル一覧・プレビューと同じ画面でコマンドを打てるので、確認しながら作業したいときに便利

> **ターミナルの画面の見方**
>
> ```
> ユーザー名@MacBook-Pro ~ %
> ```
>
> `~` は今いるフォルダ（ホーム）、`%` は「コマンドを打ってください」の合図です。

### 2-2. 絶対に覚えておくべき 3 つの Unix コマンド

ターミナルで覚えることは、まずこの **3 つだけ**で十分です。

| コマンド | 意味 | 使用例 |
|---|---|---|
| **`pwd`** | **今いるフォルダ**のフルパスを表示 | `pwd` → `/Users/taro` |
| **`ls`** | フォルダ内の**ファイル一覧**を表示 | `ls`（隠しファイルも見たいときは `ls -la`） |
| **`cd`** | フォルダを**移動**する | `cd ~/Desktop`（一つ上へ戻るときは `cd ..`） |

**まず試してみましょう。**

```
pwd
ls
cd ~/Desktop
pwd
```

> `~`（チルダ）は「自分のホームフォルダ」のショートカット。`cd ~` だけでホームに戻れます。

**覚えておくと便利な補助コマンド**

| コマンド | 用途 | 例 |
|---|---|---|
| `mkdir` | フォルダを作る | `mkdir my-folder` |
| `open .` | 今いるフォルダを Finder で開く | `open .` |

### 2-3. 研修用のプロジェクトフォルダを準備する

1-6 で学んだ「フォルダ単位の運用」を実践します。研修専用のフォルダを作って、そこに移動します。

```
mkdir -p ~/Desktop/claude-training
cd ~/Desktop/claude-training
pwd
```

`pwd` の表示が `/Users/<ユーザー名>/Desktop/claude-training` になっていれば成功です。

> **重要**：Claude Code はこのフォルダ配下のファイルを自由に読み書きします。**業務の本番ファイルがあるフォルダで直接起動せず、必ず専用フォルダで作業**してください。

### 2-4. Claude Code をインストールする（curl ネイティブインストーラ）

Anthropic が現在推奨しているのは、`curl` で公式スクリプトを取得して実行する **ネイティブインストール** です。次の 1 行をターミナルに貼り付けて Enter するだけです。

```
curl -fsSL https://claude.ai/install.sh | bash
```

> **このコマンドは何をしている？**
> - `curl -fsSL <URL>` … 指定 URL のインストール用スクリプトを取得する
> - `| bash` … 取得したスクリプトを実行する
> - 結果として、自分の Mac 用の `claude` が `~/.local/bin/claude` に置かれます

**なぜ `brew install`（Homebrew）ではなく Native install なのか？**

| 観点 | Native install（curl・推奨） | Homebrew（brew install） |
|---|---|---|
| 事前準備 | **不要**（Node.js も Homebrew も要らない） | Homebrew のインストールが先に必要 |
| 更新 | **起動時に自動で最新版へ更新** | 自分で `brew upgrade` が必要 |
| 公式の位置づけ | **現在もっとも推奨されている方法** | 代替手段の一つ |
| トラブル | 少ない | 環境差でハマることがある |

➡ ひとことで言うと「**準備がいらず、勝手に最新に保たれ、公式が一番すすめていて、トラブルが少ない**」から Native install を使います。

**インストールできたか確認**

ターミナルを **一度閉じてから開き直し**、次を実行します。

```
claude --version
```

バージョン番号が出れば成功です。

> **`zsh: command not found: claude` と出たら**：PATH（パス＝コマンドの置き場所をPCに教える設定）が通っていない可能性があります。次を実行してターミナルを開き直してください。
>
> ```
> echo 'export PATH=$HOME/.local/bin:$PATH' >> ~/.zshrc
> source ~/.zshrc
> claude --version
> ```
>
> それでもダメなら `claude doctor` を実行すると、原因の診断結果がまとめて表示されます（→ 付録 E）。

### 2-5. 初回ログインと起動

研修用フォルダで Claude Code を起動します。

```
cd ~/Desktop/claude-training
claude
```

初回起動の流れ：

1. 認証方法の選択 → **「Claude.ai Subscription」** を矢印キーで選んで Enter
2. ブラウザが自動で開く → **会社の Google Workspace アカウント**でログイン
3. ブラウザで「承認する」を押す
4. ターミナルに戻ると、Claude Code のプロンプトが表示される

起動できたら、Claude Code の中で `/init` を実行します。

```
/init
```

> `/init` は、今のフォルダの内容を分析して設定ファイル `CLAUDE.md` を自動生成します。プロジェクトを始めるときに実行しておくと、以降の精度が上がります。生成された `CLAUDE.md` は手で編集でき、中身のおすすめサンプルは **付録 H** にあります。

### 2-6. VS Code をインストールして確認用の窓にする

![Visual Studio Code](https://code.visualstudio.com/assets/docs/getstarted/userinterface/hero.png)
*出典: [Microsoft — VS Code User Interface](https://code.visualstudio.com/docs/getstarted/userinterface)*

Claude Code が作った Markdown ファイルを整形表示で確認するために、VS Code を入れます（**Claude Code の操作には使いません。あくまで確認・プレビュー用**です）。

**インストール**

1. <https://code.visualstudio.com/> にアクセス
2. 「**Download for macOS**」をクリックして `.zip` をダウンロード
3. ダウンロードした zip をダブルクリックで解凍
4. 出てきた「**Visual Studio Code.app**」を `/Applications`（アプリケーションフォルダ）へドラッグ＆ドロップ
5. ダブルクリックで起動（初回は「開いてもよろしいですか？」→「開く」）

**日本語化**

1. `Cmd + Shift + P` でコマンドパレットを開く
2. 「`Configure Display Language`」と入力して選択
3. 「`Install additional languages...`」→ 一覧から「**日本語 (ja)**」の `Install`
4. 右下の「Restart」をクリック → 日本語で再起動

**ターミナルから `code` で開けるようにする**

1. `Cmd + Shift + P` →「`shell command`」と入力
2. 「**Shell コマンド: PATH 内に 'code' コマンドをインストールします**」をクリック
3. Mac のログインパスワードを入力
4. ターミナルを開き直して確認：

```
code --version
```

### 2-7. なぜ Markdown（.md）で作るのか

本研修では、Claude Code に作らせるドキュメント（議事録・提案書・比較表など）を **Markdown ファイル（拡張子 `.md`）** で扱います。

- **AI と相性がいい** — 中身は記号付きテキストなので、Claude が生成・修正しやすい
- **専用ソフトが不要** — VS Code・テキストエディット・ターミナルのどれでも開ける
- **PDF・Word・スライドへ変換しやすい** — 一つの `.md` から用途別に展開できる
- **差分が見やすい** — 修正前後の違いが行単位で分かる

**ワークフローの基本**：`Markdown で下書き → VS Code でプレビュー確認 → 必要なら PDF/Word/スライドに変換`

**プレビュー方法（VS Code）**

```
code 競合比較表.md      # ファイル単体を開く
code .                  # 今いるフォルダ全体を開く
```

VS Code で `.md` を開いたら、`Cmd + K` を押してから `V` を押すと、右側に整形プレビューが並びます。Claude Code に修正を頼むたび、プレビューも自動で更新されます。

### 2-8. 自動承認モードとモード切替（Shift + Tab）

Claude Code は、ファイル変更やコマンド実行のたびに「Yes / No」の許可を求めます。毎回許可するのは手間なので、モードを使い分けます。

**起動時に自動承認モードで始める（おすすめ）**

```
claude --enable-auto-mode
```

**起動中に切り替える**：`Shift + Tab` を押すたびにモードが順番に変わります。

| モード | 動作 | 用途 |
|---|---|---|
| 通常（Ask before edit） | すべての操作で許可を求める | 1 つずつ慎重に確認したいとき |
| **自動承認（Edit automatically）** | ファイル編集は自動、コマンドは確認 | **本研修ではこれを推奨** |
| プランモード（Plan mode） | **実行せず計画だけ提示** | 大きな作業の前に内容を確認したいとき |

> **プランモードの使いどころ**：「いきなり全部任せるのは不安」なときは、まずプランモードで計画を見せてもらい、納得したら通常／自動承認に戻して実行します。

### 2-9. 荒い指示でも大丈夫 — Claude Code が要件をヒアリングしてくれる（プランモードが重要）

チャットでは「指示が曖昧だと、AI が勝手に解釈して的外れな答えを返す」ことがよくあります。Claude Code は違います。**ざっくりした（荒い）タスクを投げると、足りない情報を逆に質問し、要件を確認してから動く**ように仕向けられます。

たとえば、こんな荒い指示で十分です。

```
来週の営業会議に使う、先月の問い合わせ件数のレポートを作って。
```

すると Claude Code は、いきなり作り始めずに——

- 「元データはどのファイル／どこにありますか？」
- 「集計は日別・週別のどちらにしますか？」
- 「出力は Markdown・PDF・スライドのどれにしますか？」

——のように、**作る前に要件をヒアリング**してくれます。あなたは思いついたことを荒く投げて、聞かれたことに答えていくだけで、認識のズレを事前に潰せます。

> **コツ**：最初から完璧な指示を書こうとしなくて大丈夫です。「**まず荒く投げて、質問に答えながら固める**」のが、チャットにはないエージェントならではの進め方です。

**そして、大きな作業ほど「プランモード」が重要です。**

2-8 で触れたプランモード（`Shift + Tab` で切替）は、**ファイルもコマンドも一切変更せず、「何をどうやるか」の計画だけを先に出す**モードです。

```
（Shift + Tab でプランモードにしてから）
先月の問い合わせ件数を集計して、営業会議用のレポートにしたい。
どう進めるか、計画を立ててください。
```

`荒く投げる → 要件ヒアリングで方針が固まる → プランモードで計画を確認 → 納得したら実行` という流れにすると、想定外の変更を防げます。計画に問題がなければ「**この計画で進めてください**」と返すだけで、すぐに作業を始めてくれます。

> **作者も「まず計画」から**：Claude Code を作った Boris Cherny（Anthropic、Claude Code 開発責任者）も、**ほぼ毎回プランモードで始める**（`Shift + Tab` を 2 回）と公言しています。本人いわく「良い計画こそが、後のトラブルを防ぐ（"A good plan is really important to avoid issues down the line"）」。1 日に 20〜30 件の変更をさばくプロでも、いきなり実行せず**まず計画**から入ります。出典：[How Boris Uses Claude Code](https://howborisusesclaudecode.com/)

![How Boris Uses Claude Code](https://howborisusesclaudecode.com/og.png)
*出典: [How Boris Uses Claude Code](https://howborisusesclaudecode.com/)*

> **この振る舞いを“毎回”安定させるには**：「勝手に決めず、まず確認する」という動き方は、プロジェクトに `CLAUDE.md`（Claude が毎回読む設定ファイル）を置いて指示しておくと安定して効きます。おすすめのサンプルを **付録 H** に用意しました。

### 2-10. 動作確認ハンズオン — 競合比較表を作って PDF にする

![Claude Code ターミナルデモ](https://raw.githubusercontent.com/anthropics/claude-code/main/demo.gif)
*出典: [GitHub — anthropics/claude-code](https://github.com/anthropics/claude-code)*

Claude Code（`claude --enable-auto-mode` で起動）に、次をそのまま入力します。

```
以下の3社のWebサイトを読んで、人事評価サービスの競合比較表を作ってください。
比較項目は「サービス概要」「主な機能」「ターゲット企業規模」「料金」とします。
markdownファイルとして保存してください。
- https://smarthr.jp/
- https://www.kaonavi.jp/
- https://hcm-jinjer.com/
```

AI が 3 社のサイトをリアルタイムに読み取り、比較表を `.md` で保存します。続けて PDF 化も頼めます。

```
先ほど作った競合比較表の markdown ファイルを PDF に変換してください
```

確認は VS Code（`code .`）か、Finder で `.md`／`.pdf` を選んで **スペースキー（QuickLook）** が手軽です。

> **成果物の置き場所**：研修中に作ったファイルは、研修用フォルダ（`~/Desktop/claude-training`）など**作業用フォルダの中**に置きます。業務の本番フォルダに混ぜないでください。

---

## 休憩（10 分）

---

## Part 3　記憶を持たせる（約 60 分）｜QA2（M さん）メモリ機能

### 3-1. QA2（M さん）の悩み

> **質問**：Claude と壁打ちしながら、連絡手段（メール・Slack・チャットワーク）× 議事録 × 関連資料（広告レポート等）× 日次の業界ニュースを読ませて、背景や時系列を踏まえた回答・マーケ改善案を出したい。でも、**データをためても学習・理解させて回答させることができず**、会話の中で別案件が混ざったり、時系列を理解しない回答になったりして苦戦している。

この悩みの正体は「**Claude に記憶がない**」ことです。通常の Claude Code は、セッション（会話）を閉じると内容を忘れます。だから毎回ゼロからになり、案件が混ざり、時系列も追えません。

**解決策は「2 階建ての記憶」を使うこと**です。

### 3-2. 2 階建ての記憶

Claude Code には、性格の違う 2 つの記憶の仕組みがあります。

| | ① claude-mem（プラグイン） | ② 標準メモリ（最初から組み込み） |
|---|---|---|
| ひとことで | 会話のたびに**裏で勝手にメモを取る秘書** | 「これは大事」と判断したとき**意図的に書き留める手帳** |
| インストール | 必要 | 不要（すでに動いている） |
| 書くタイミング | 応答が終わるたび毎回（自動） | 重要な確定事項が出たときだけ |
| 書く内容 | 作業ログ（多い） | 結論・判断・確定事実だけ（厳選） |
| 思い出し方 | 同じフォルダで開くと過去ログが自動で蘇る | 毎回「目次」が自動で読まれる |

この 2 つで「**細かい作業ログ（①）**」と「**重要な結論（②）**」がきれいに分業されます。M さんの「ためたデータを活用させたい」は、この 2 つで解決します。

### 3-3. ① claude-mem を入れる（5 分）

![claude-mem](https://opengraph.githubassets.com/1/thedotmack/claude-mem)
*出典: [GitHub — thedotmack/claude-mem](https://github.com/thedotmack/claude-mem)*

claude-mem は「Claude Code に日記帳を持たせる」プラグインです。

```
（Claude Code を起動した状態で、入力欄に順に打つ）
/plugin marketplace add thedotmack/claude-mem
/plugin install claude-mem
```

入れ終わったら、一度 `exit` して `claude` を立ち上げ直します（**再起動が必要**）。次回からは、新しいセッションの冒頭に過去の記憶（`$CMEM ...` という表示）が自動で出てきます。

> ⚠️ `npm install -g claude-mem` という別コマンドもありますが、こちらはプラグインとしては動きません。必ず上の `/plugin` コマンドを使ってください。

**動作確認**

- 新しいセッションの冒頭に `# $CMEM <フォルダ名> ...` が出れば成功
- ブラウザで <http://localhost:37777> を開くと、たまっていく記憶を見られます

### 3-4. claude-mem の超重要ルール — フォルダ名で仕分けされる

claude-mem は「**いま開いているフォルダ名（末尾）**」だけを見て記憶を仕分けます。ここが M さんの「案件が混ざる」問題に直結します。

| 今いるフォルダ | claude-mem が見るラベル |
|---|---|
| `/Users/me/work/client-a` | `client-a` |
| `/Users/me/private/client-a` | `client-a` ← **混ざる！** |
| `/Users/me/work/client-b` | `client-b` |

➡ **案件ごとにフォルダ名をユニークにすれば、記憶は混ざりません。** これが「会話の中で別案件が混ざる」への根本対策です。

**別フォルダの記憶を横断して探したいときは、明示的に頼みます。**

```
/mem-search 〇〇の解決方法
```

「前に似たことをやったよね？」と聞くと、Claude が自動で横断検索することもあります。

### 3-5. ② 標準メモリを使う — 重要な事実を固定する

標準メモリは、特別なコマンド不要。会話の中でこう頼むだけです。

```
これは重要なので覚えておいてください：
A社の主要KPIは「CPA 8,000円以下」「月予算300万円」。担当媒体は Google広告 と Meta広告。
```

すると Claude が、プロジェクトの記憶フォルダに Markdown ファイルを作り、目次（`MEMORY.md`）に 1 行追記します。次回以降のセッションでは目次が自動で読まれ、関係しそうなときに本文を読み返します。

> 標準メモリは **プロジェクトフォルダの外**（`~/.claude/projects/.../memory/`）に保存されます。成果物に AI の痕跡を残さずに済むのが利点です。

### 3-6. ハンズオン — 記憶の挙動を体験する（10 分）

**演習 1：記憶を書き込む**

```
cd ~/Desktop/claude-training
claude
（質問）このフォルダで研修資料を作りたいです。タイトル候補を3つ教えて。
（応答後）exit
```

**演習 2：同じフォルダで思い出すか確認**

```
claude
（冒頭に $CMEM claude-training ... が出れば成功）
前回どんなタイトル候補を出してくれましたか？
→ 覚えていれば成功
```

**演習 3：別フォルダでは引き継がれないことを確認**

```
cd ~/Documents          # 別の場所へ移動
claude
前回どんなタイトル候補を出してくれましたか？
→「わかりません」と返るはず（フォルダ名が違うため）
```

**演習 4：横断検索を試す**

```
/mem-search タイトル候補
→ 別フォルダの内容も含めて検索される
```

### 3-7. 落とし穴チェックリスト

| 状況 | どうなるか |
|---|---|
| 同じフォルダで再起動 | ⭕ 自動で思い出す |
| 別フォルダで新セッション | ❌ 自動では思い出さない（`/mem-search` なら可） |
| **同名の別フォルダ**で起動 | ⚠️ 記憶が混ざる（要注意） |
| ホーム直下（`cd ~`）から起動 | ⚠️ `unknown-project` に集約されてごちゃ混ぜに |
| 機密情報を扱うフォルダで無設定使用 | ⚠️ 会話の要約が保存される。注意が必要 |

> **機密情報を扱うとき**：そのプロジェクトでは claude-mem を無効化する（`~/.claude-mem/settings.json` の `CLAUDE_MEM_EXCLUDED_PROJECTS` に追加）か、扱う情報に注意してください。

### 3-8. QA2（M さん）への答え — 混線と時系列誤認をなくす運用

M さんの「データをためても活用できない／案件が混ざる／時系列を理解しない」は、次の運用で解決できます。

1. **案件ごとにフォルダを分ける**（フォルダ名をユニークに）→ claude-mem が案件単位で記憶を仕分け、**混線がなくなる**。
2. **その案件で動かない前提は、標準メモリに固定する**（KPI・担当媒体・予算・関係者・直近の意思決定など）→ 毎回読み直されるので、**時系列・背景を踏まえた回答**になる。
3. **資料はその案件フォルダに集約**（広告レポート・議事録・関連資料）→ Claude Code が同じフォルダ内を直接読めるので、壁打ちの精度が上がる。
4. **横断して過去事例を探したいときだけ `/mem-search`** を使う。

> つまり「秘書（claude-mem）が日々の作業ログを自動で残し、自分の手帳（標準メモリ）に大事な結論だけ書き留める」。この 2 階建てを案件フォルダ単位で回すのが、M さんの理想形への最短ルートです。

### よく使うスラッシュコマンド（1 日目の持ち帰りチートシート）

スラッシュコマンド（`/` で始まる命令）は、Claude Code の機能を直接呼び出す近道です。**全部を覚える必要はありません。** まずは下の“よく使う”だけ押さえれば十分です（コマンド一覧はいつでも `/help`、基本リファレンスは付録 C にあります）。

| コマンド | いつ使う？ |
|---|---|
| `/help` | コマンドや使い方を忘れたとき（一覧を表示） |
| `/clear` | 別の作業に切り替えるとき（会話をリセット。CLAUDE.md やメモリの記憶は残る） |
| `/compact` | 会話が長くなって返答が鈍ったと感じたとき（要約して立て直す） |
| `/rewind` | 操作をやり直したいとき（直前の状態へ巻き戻す。`Esc` を 2 回でも可） |
| `/resume` | 前回の続きを再開したいとき（`claude -c` でも可） |
| `/model` | 速さ重視↔深さ重視で AI モデルを切り替えたいとき |
| `/usage` | いまどれくらい使ったか・上限を確認したいとき（別名 `/cost`） |
| `/plan` | 大きな作業の前に計画だけ見たいとき（プランモード。`Shift + Tab` でも可 → 2-9） |
| `/memory` | CLAUDE.md（毎回読む設定ファイル）を編集したいとき（→ 付録 H） |
| `/deep-research <調べたいこと>` | 出典付きで自動的にネット調査させたいとき |
| `/voice` | キーボードではなく声で入力したいとき（音声入力） |

> **⭐ おすすめ：`/remote-control`（別名 `/rc`）— PC の作業をスマホから操作・確認する**
> 長い作業を PC で走らせている間に席を外しても、**スマホや別のブラウザから同じセッションを操作・確認**できます。`/remote-control` を実行すると QR コードが表示されるので、**Claude モバイルアプリ**（アプリ未所持なら `/mobile` で入手用 QR を表示）で読み取るだけ。ファイル・MCP 接続・プロジェクトはすべて **PC 側のまま**で、クラウドには移りません。第 2 回の「長い自動化を仕掛けて、外出先で結果を確認する」と相性抜群です。
> ※ 利用には有料プラン（Pro / Max / Team など）と claude.ai へのログインが必要です（本研修の前提どおり）。リサーチプレビュー機能。出典：[Remote Control — Claude Code Docs](https://code.claude.com/docs/ja/remote-control)

### 第 1 回のまとめ・宿題

**できるようになったこと**

- ✅ なぜチャットではなく Claude Code（エージェント）なのかを説明できる
- ✅ Mac に Claude Code を Native install し、研修用フォルダで起動できる
- ✅ VS Code で生成物を整形プレビューできる
- ✅ 荒い指示でも、要件ヒアリングとプランモードで大きな作業を安全に任せられる
- ✅ claude-mem と標準メモリの 2 階建てで「覚えさせる」ことができる
- ✅ よく使うスラッシュコマンド（`/help`・`/clear`・`/compact`・`/rewind` ほか、スマホ操作の `/remote-control`）を使い分けられる

**宿題**：自分の業務フォルダを 1 つ「案件名のユニークなフォルダ」として作り、その中で Claude Code を起動して、覚えておいてほしい前提（KPI・関係者・直近の決定など）を 3 つ標準メモリに登録してみる。

---

# 第 2 回（3 時間）：業務を仕組み化する｜QA3（O さん）自動化させる

> ゴール：「議事録 → 提案資料 → 会議候補日 → 会議案内メール下書き → リマインド」までの一連の流れを、**Claude Code に任せて自動で回す**ところまで体験する。

### QA3（O さん）の悩み

> **質問**：今はほとんどの業務を Claude のチャット上で処理していて、都度やり取りするので**同じ作業を毎回ゼロから指示**している。打ち合わせ後の流れ（議事録 → 資料作成 → 会議設定 → スケジュール設定 → 先方への送付メール下書き → リマインド…）を、**毎回チャットで指示するのではなく、一連の流れとして自動で回したい**。

この悩みは、Claude Code の本領そのものです。第 2 回でまるごと解決します。

| パート | 時間の目安 | 内容 |
|---|---|---|
| Part 1 | 約 30 分 | 座学：仕組み化の設計思想 |
| Part 2 | 約 45 分 | 下ごしらえ：MCP 接続＆連鎖 Skill の準備 |
| 休憩 | 10 分 | |
| Part 3 | 約 90 分 | ハンズオン：6 ステップを通す |
| Part 4 | 約 20 分 | ふりかえり・自分の業務への展開 |

---

## Part 1　座学：仕組み化の設計思想（約 30 分）

### 1-1. 「毎回ゼロから指示」をやめる 3 つの道具

O さんの「毎回ゼロから」を卒業するには、3 つの道具を組み合わせます。

| 道具 | 役割 | たとえると |
|---|---|---|
| **Skills（スキル）** | やり方（手順・ノウハウ）を覚えさせる | レシピ本 |
| **MCP** | メール・カレンダー・課題管理などの外部サービスにつなぐ | 食材を仕入れる業者 |
| **スケジュール** | 決まった時刻・条件で自動起動する | タイマー |

- **Skills だけ**だと「やり方は知っているが、外のデータに手が届かない」
- **MCP だけ**だと「手は届くが、手順がバラバラ」
- **両方＋スケジュール**で、初めて「**決まった流れが、決まったタイミングで、勝手に回る**」状態になります

### 1-2. なぜ Linear ＋ Claude Code なのか — 「managed agent」を“作らずに”手に入れる

自動化の世界には **managed agent（マネージド・エージェント）** という考え方があります。**「課題（タスク）を AI に割り当てると、AI が自分で作業を進め、進捗やステータスを返してくれる」** 仕組みのことです。人間の同僚にタスクを振るのと同じ感覚で AI に仕事を任せられる、というイメージで、もともとはエンジニアの開発自動化で広まりました。

本研修で今日作る「**Linear に議事録を登録 → Claude Code が資料作成や会議調整を進める**」も、この managed agent を**業務向けに再現したもの**です。

同じことを実現する選択肢はいくつかあります。選ぶ基準は、**①すでに払っている Claude サブスク（定額）で動くか**、**②自分でシステムを作る必要があるか**、の 2 つです。

| 選択肢 | 何か | ① サブスクで動く？ | ② 自分でシステムを作る必要 |
|---|---|---|---|
| Anthropic Claude Managed Agents | Anthropic 公式のホスト型サービス | ✕（API 従量課金） | 不要だが開発者向け |
| OpenClaw / OpenCode | オープンソースのエージェント | △（基本は API 課金） | 設定が必要・エンジニア向け |
| Multica | オープンソースの managed agents 基盤（Linear 風の課題ボード） | ⭕（裏で Claude Code を使えば） | **必要（基盤の構築・運用）** |
| **Linear ＋ Claude Code（＋MCP）** | 既存の Linear に Claude Code を MCP でつなぐ | **⭕** | **不要（つなぐだけ）** ← 本研修 |

![Multica](https://opengraph.githubassets.com/1/multica-ai/multica)
*（参考）「managed agents 基盤」の一例。出典: [GitHub — multica-ai/multica](https://github.com/multica-ai/multica)*

- **API 従量課金**（使った分だけ別料金）の仕組み（Anthropic Managed Agents や多くのオープンソース・エージェント）は、すでに Claude の有料プランに入っているなら**もったいなく、料金も読みづらい**です。
- **サブスクのまま**動かせるのは、実質 **Multica** か **Linear ＋ Claude Code**。
- ただし **Multica は自分でシステムを立てて運用**する必要があり、非エンジニアには重い。
- だから本研修では、**すでにある Linear を司令塔にして、MCP で Claude Code をつなぎ、“managed agent っぽい仕組み”を“作らずに”再現**します。

> **「使い慣れた Google ToDo（Google Tasks）ではダメ？」**：Google Workspace を使っているなら、課題管理も Google ToDo で揃えたくなるところです。ただし Google ToDo には、Linear のような **公式の“すぐ繋がる”MCP が用意されていません**。使うには **自分で MCP サーバーを立てて運用する必要があり、構築が大変**です（まさに上の②「自分で作る必要がある」側に当たります）。一方 Linear は **公式の MCP（`https://mcp.linear.app/sse`）に OAuth でつなぐだけ**で済みます。だから本研修では「作らずに繋がる」Linear を司令塔に選びました。

> この分野は新しいツールが次々に登場します。製品名を覚えることより、**「サブスク（定額）で動くか × 自分で作らなくて済むか」という選び方の軸**を持ち帰ってください。

### 1-3. Linear を「司令塔」にする発想

![Linear](https://linear.app/static/og/homepage.jpg)
*出典: [Linear](https://linear.app)*

今回の自動化では、課題管理ツール **Linear（リニア）** を **司令塔（タスクの入れ物）** として使います。

- **1 つの議事録 ＝ 1 つの Linear チケット**として登録する
- チケットの **ステータス（Todo → In Progress → Done）** で、自動化がどこまで進んだかを管理する
- 作った資料の URL や、次にやることも、すべてチケットに集約する

こうすると「いま何がどこまで進んでいるか」が一目で分かり、複数の案件が並行しても混乱しません。チャットの「毎回ゼロから」とは正反対の、**仕組みとして回る**状態です。

### 1-4. 今日作る一連フローの全体像（6 ステップ）

```
① 議事録を用意
      │（議事録＋「次にしてほしいこと」）
      ▼
② Linear にチケット登録   ← 司令塔に投入（直接 or Slack から）
      │
      ▼
③ skill①「meeting-to-doc」を呼び出し
      │  議事録 → 提案資料の草案 → Google Drive に保存
      │  → 保存先 URL を Linear チケットに追記 → 完了で次へ
      ▼
④ skill②「meeting-scheduler」を呼び出し
      │  Google Calendar の空きから候補日3つ → 仮予定で登録
      │  → 会議案内メールの下書きを作成 → Linear のステータス更新
      ▼
⑤ 人の目で下書きを確認 → 送信   ← ここだけ人間（安全のため）
      │
      ▼
⑥ 会議の前日にリマインド   ← カレンダー標準 or Claude のスケジュール機能
```

> **ポイント**：完全自動ではなく、⑤の「送信」だけは**必ず人が確認**します。AI は下書きまで、最終判断は人。これが業務で安心して使うコツです（human-in-the-loop ＝ 人を間に挟む）。

---

## Part 2　下ごしらえ：MCP 接続＆連鎖 Skill の準備（約 45 分）

### 2-1. MCP とは — AI 業界の「電源プラグ規格」

![Model Context Protocol](https://www-cdn.anthropic.com/images/4zrzovbb/website/3aabd8804251c0364cbde9d2e4be6dc8e8c2faec-2880x1620.png)
*出典: [Anthropic — Introducing the Model Context Protocol](https://www.anthropic.com/news/model-context-protocol)*

> **MCP（Model Context Protocol）** とは、AI エージェントが外部サービスにアクセスするための共通規格です。Anthropic が提唱し、業界標準になりつつあります。

```
   AIエージェント側      ⟷      MCPサーバー側
   ・Claude Code               ・Google Drive
   ・Cowork           ←→       ・Gmail
   ・Claude.ai                 ・Google Calendar
                               ・Linear / Slack ...

   ↑ どんなエージェントでも、同じプラグ規格でつながる ↑
```

一度 MCP に対応したサービスは、Claude Code / Cowork / Claude.ai のすべてから使えます。

### 2-2. Skills と MCP の違い（混同しやすいので注意）

| | Skills | MCP |
|---|---|---|
| 役割 | やり方を教える（手順・ノウハウ） | 外部サービスにつなぐ（接続・取得・実行） |
| 中身 | Markdown の指示書＋テンプレ | サーバープログラム（API の窓口） |
| たとえ | **レシピ本** | **食材を仕入れる業者** |

両方そろって初めて、業務が最初から最後まで回ります。

### 2-3. 4 つの MCP を接続する（`/mcp` を使う方法）

Claude Code を起動した状態で、入力欄に `/mcp` と打つと MCP 管理画面が開きます。ここから「**Add a server（サーバーを追加）**」で 1 つずつ接続します。OAuth（認証）画面まで誘導してくれるので、初めてでも迷いません。

**共通の手順（毎回これを繰り返す）**

1. `claude` を起動 →（すでに起動済みならそのまま）
2. `/mcp` と打って Enter → 管理画面が開く
3. 「**Add a server**」を選んで Enter
4. 名前を入力（例：`google-drive`）
5. 接続先 URL を入力
6. ブラウザが自動で開く → **会社の Google Workspace アカウント**で「許可」
7. 「接続しました」と出たら Claude Code に戻る
8. もう一度 `/mcp` を開き、ステータスが **`connected`** になっているか確認

**今日つなぐ 4 つの MCP**

| MCP | 名前（例） | 用途 |
|---|---|---|
| Google Drive | `google-drive` | 資料の保存・読み込み |
| Gmail | `gmail` | メールの下書き作成・検索 |
| Google Calendar | `google-calendar` | 予定の確認・候補日の作成 |
| **Linear** | `linear` | チケット管理（司令塔）。接続先：`https://mcp.linear.app/sse` |

> Google 系 3 つの接続先 URL は講師から共有されます。Linear だけは `https://mcp.linear.app/sse` を入力します。

**（任意）Slack も使う場合**：Slack MCP を接続すると、Slack のメッセージから Linear にチケットを登録する流れも試せます（後述）。

> **セキュリティの注意**：OAuth 画面では「**何の権限を許可しているか**」を必ず読んでください。第三者の出どころ不明な MCP サーバーは使いません（公式・社内・信頼できるものだけ）。

**全部つながったか確認**

```
/mcp
```

```
google-drive     connected
gmail            connected
google-calendar  connected
linear           connected
```

すべて `connected` になっていれば下ごしらえ完了です。`needs auth` で止まっていたら、その行を選んで「Authenticate」をやり直します（→ 付録 E）。

### 2-4. 連鎖する 2 つのスキルを用意する

O さんの「終わり次第、次のスキルを呼ぶ」を実現するため、スキルを **2 本に分けて連鎖**させます。役割を分けておくと、片方だけ直したいときに楽です。

| スキル | やること | 使う MCP |
|---|---|---|
| **`meeting-to-doc`** | 議事録 → 提案資料の草案 → Google Drive 保存 → URL を Linear に追記 → 完了で次を促す | Drive・Linear |
| **`meeting-scheduler`** | Calendar の空きから候補日 3 つ → 仮予定登録 → 会議案内メール下書き → Linear ステータス更新 | Calendar・Gmail・Linear |

**スキルの置き場所と構造**

スキルは「フォルダ＋ `SKILL.md`（指示書）」の形で置きます。プロジェクト配下に置けば、そのプロジェクトで発火します。

```
~/Desktop/claude-training/
└── .claude/
    └── skills/
        ├── meeting-to-doc/
        │   └── SKILL.md
        └── meeting-scheduler/
            └── SKILL.md
```

`SKILL.md` の冒頭には **frontmatter**（メタデータ）を書きます。`description` に「いつ使うか」を具体的に書くのがコツです（Claude が「このスキルを使うべきか」を判断する材料になる）。

```yaml
---
name: meeting-to-doc
description: 営業議事録から提案資料の草案を作り、Google Drive に保存し、保存先 URL を Linear チケットに追記する。議事録・打ち合わせメモから資料を起こすときに使う。
---
```

> スキルの中身（SKILL.md の全文サンプル）は **付録 D** に載せています。ハンズオンではこれをコピーして使います。本文では「何をする指示書か」を理解しておけば十分です。

> **Skill / プラグイン / MCP / サブエージェントの整理**：スキル＝やり方の指示書、プラグイン＝スキルなどをまとめて配布する仕組み（claude-mem もプラグイン）、MCP＝外部サービスへの接続、サブエージェント＝重い作業を別の Claude に切り出して並行処理させる仕組み。今日の主役は **スキル × MCP** です。

---

## 休憩（10 分）

---

## Part 3　ハンズオン：6 ステップを通す（約 90 分）

`claude --enable-auto-mode` で起動し、研修用フォルダ（`~/Desktop/claude-training`）で進めます。

### ステップ① 議事録を用意する

今日は再現性のため、用意済みの **サンプル議事メモ**（人事評価制度コンサルの営業議事録）を入力に使います。研修用フォルダに `議事メモ.md` として置いてください（講師が配布）。

> **実務での議事録の取り方（3 通り）** — 今日はサンプルを使いますが、現場では次のように取ります。
> 1. **Google Meet の文字起こし**：会議後に生成される文字起こしをコピーして貼り付ける
> 2. **read.ai などの会議記録ツール**：自動で要約・文字起こしを作るツールの出力を使う
> 3. **Claude の音声入力**：その場で話した内容を音声入力で議事録化する
>
> どの方法でも、できあがった議事録を次のステップ以降に流すだけで、同じ自動化が回ります。

### ステップ② Linear にチケットを登録する

議事録を「司令塔」Linear に登録します。このとき、**次にしてほしい命令文（依頼内容）も一緒に書く**のがコツです。登録方法は 2 通りあります。

**方法 A：Claude Code から直接 Linear に登録**

```
議事メモ.md の内容をもとに、Linear にチケットを1件作成してください。
- タイトル：「グリーンテック様 人事評価制度コンサル提案」
- 説明：議事メモの要点（先方の課題・規模・温度感）を5行で要約
- 説明の末尾に「次にしてほしいこと」として以下を追記：
  「この議事録から提案資料の草案を作成し、Google Driveに保存して、その URL をこのチケットに追記する」
- ステータス：Todo
作成後、チケットのURLを教えてください。
```

**方法 B：Slack から登録する**（Slack MCP を接続している場合）

Slack の特定チャンネルに議事録の要点を投稿し、その内容から Linear チケットを作る流れです。

```
Slack の #議事録 チャンネルの最新の投稿を読んで、
その内容をもとに Linear にチケットを1件作成してください。
（タイトル・説明・次にしてほしいことは方法Aと同じ要領で）
```

> どちらでも結果は同じ「Linear に 1 議事録 1 チケットができる」状態です。普段 Slack で報告している人は B、Claude Code で完結したい人は A が向いています。

### ステップ③ skill① `meeting-to-doc` を呼び出す

チケットができたら、1 本目のスキルを呼び出します。

```
/meeting-to-doc

対象は、さきほど作成した Linear チケット「グリーンテック様 人事評価制度コンサル提案」です。
チケットに紐づく議事録（議事メモ.md）をもとに、提案資料の草案を作成してください。
```

スキルが順に実行します（中身は付録 D）。

1. 議事録を読み、**提案資料の草案**（課題整理・提案方針・想定スケジュール・概算）を Markdown で作成
2. **Google Drive に保存**（指定フォルダ。ファイル名は案件名で）
3. 保存先の **URL を Linear チケットに追記**
4. 「資料の草案ができました。次に会議調整（`meeting-scheduler`）を実行しますか？」と**次のスキルを促す**

> VS Code で草案 `.md` を `Cmd + K → V` でプレビューし、内容をざっと確認しましょう。

### ステップ④ skill② `meeting-scheduler` を呼び出す

続けて 2 本目のスキルを呼びます。

```
/meeting-scheduler

対象は同じ Linear チケットです。先方との提案会議を設定したいです。
自社メンバー（田中・佐藤）の Google Calendar を見て、来週の平日で全員が空いている候補日を3つ選んでください。
```

スキルが順に実行します。

1. **Google Calendar を確認**し、自社メンバーの空きから**候補日を 3 つ**選ぶ
2. その 3 つを **「仮」予定としてカレンダーに登録**（タイトルに【仮】を付ける）
3. **会議案内メールの下書き**を Gmail に作成（候補日 3 つを提示し、先方に都合を伺う文面。**送信はしない**）
4. **Linear のステータスを更新**（Todo → In Progress）し、候補日とメール下書きの状況をチケットに追記

### ステップ⑤ 人の目で下書きを確認して送信する

ここは**人間の出番**です。

1. Gmail の「下書き」フォルダを開く
2. 会議案内メールの宛先・文面・候補日を**自分の目で確認**し、必要なら手直し
3. 問題なければ**自分で送信**する

> なぜ送信を自動にしないのか：宛先や文面のミスは取り返しがつきません。**AI は下書きまで、最終の送信判断は人**、と切り分けるのが業務で安全に使うコツです。

### ステップ⑥ 会議の前日にリマインドする

最後に「前日リマインド」を仕掛けます。やり方は 2 通り。**両方知っておくと使い分けられます。**

**方法 A：Google カレンダーの標準リマインド（いちばん簡単）**

会議予定を確定登録するとき、カレンダー側の通知（例：開始 1 日前）を設定しておきます。Claude 側は何もしなくても、Google が前日に通知してくれます。

```
確定した会議予定に、開始1日前の通知（メール通知）を設定してください。
```

**方法 B：Claude Code のスケジュール機能（`/schedule`）**

Claude Code 自身に「あとで自動実行」を仕掛けられます。リマインドメールの**下書きを前日に自動生成**させたり、関連資料のリンクを添えたりと、**カレンダー通知より中身を作り込める**のが利点です。

```
/schedule 会議の前日の朝9時に、以下を実行してください。

1. 確定した提案会議の予定をGoogle Calendarから取得する
2. 先方宛のリマインドメールの下書きを作成する
   （日時・場所・持ち物・提案資料のDriveリンクを含める）
3. Gmailの下書きに保存する（送信はしない）
```

> 勉強会では待ち時間が長いので、まず **「1分後に1回だけ」** で動作確認してから、本番用に「前日の朝9時」に置き換えます。

| | 方法 A：カレンダー標準 | 方法 B：Claude の `/schedule` |
|---|---|---|
| 手軽さ | ◎ 設定 1 回で済む | ○ プロンプトを書く |
| 中身の作り込み | △ 定型通知のみ | ◎ 文面・添付リンクまで自動生成 |
| 向いている用途 | 単純な「忘れ防止」 | リマインドメールの下書きまで欲しいとき |

これで「議事録 → 提案資料 → 会議候補 → 会議案内メール下書き → リマインド」の一連の流れが、**Linear を司令塔に、Claude Code が回せる**状態になりました。O さんの「一連の流れを自動で回したい」が実現できています。

---

## Part 4　ふりかえり・自分の業務への展開（約 20 分）

### できるようになったこと

- ✅ Skills（やり方）× MCP（接続）× スケジュール（起動条件）の役割を説明できる
- ✅ Google Drive / Gmail / Calendar / Linear の 4 つの MCP を接続できる
- ✅ 2 本のスキルを連鎖させ、議事録から会議案内メール下書きまでを自動化できる
- ✅ 「送信だけは人が確認」という安全な自動化の組み方を理解した
- ✅ 前日リマインドを、カレンダー標準と `/schedule` の 2 通りで仕掛けられる

### 宿題：自分の定型業務を 1 つ仕組み化する

次のテンプレで、自分の業務を 1 つ選んで設計してみてください。

```
- 業務名：（例：月次レポートの作成と共有）
- きっかけ（起動条件）：（例：毎月1日の朝 / 議事録ができたら など）
- 使う MCP：（Google Drive / Gmail / Calendar / Linear / Slack など）
- 一連の流れ：
   ① …を取得 → ② …で整形（どのスキル？）→ ③ …を保存/下書き → ④ Linear で進捗管理
- 人が確認する箇所：（どこを human-in-the-loop にするか）
- 期待する効果：（何分 → 何分に短縮されそうか）
```

---

# 付録

## 付録 A：3 つの Unix コマンド早見＋ mac 便利コマンド

**まず覚える 3 つ**

| コマンド | 意味 | 例 |
|---|---|---|
| `pwd` | 今いるフォルダを表示 | `pwd` |
| `ls` | ファイル一覧を表示 | `ls` / `ls -la` |
| `cd` | フォルダを移動 | `cd ~/Desktop` / `cd ..` |

**補助コマンド**

| コマンド | 用途 | 例 |
|---|---|---|
| `mkdir` | フォルダを作る | `mkdir my-folder` |
| `cat` | ファイルの中身を表示 | `cat memo.md` |
| `open` | Finder／関連アプリで開く | `open .` / `open report.pdf` |

**mac ならではの便利コマンド**

| コマンド | 用途 | 例 |
|---|---|---|
| `pbcopy` | 標準入力をクリップボードにコピー | `cat memo.md \| pbcopy` |
| `pbpaste` | クリップボードの内容を出力 | `pbpaste > memo.txt` |
| `say` | 文字を音声で読み上げ | `say "完了しました"` |
| `caffeinate` | スリープを抑制（長時間処理中に便利） | `caffeinate -d` |

> ⚠️ **`rm` は元に戻せません**：Finder のゴミ箱を経由せず即削除されます。特に `rm -rf <パス>` は配下を問答無用で消すので、打つ前に `ls <パス>` で中身を確認する習慣を。

## 付録 B：settings.json（コマンド許可設定）

頻繁に使う安全なコマンドを事前許可し、致命的な操作だけをブロックしておくと、確認ダイアログに止められず作業がスムーズです。最もシンプルなのは、ユーザー設定 `~/.claude/settings.json` に書く方法です。

```json
{
  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": [
      "Bash(*)",
      "WebFetch"
    ],
    "deny": [
      "Bash(rm -rf /)",
      "Bash(rm -rf / *)",
      "Bash(rm -rf ~)",
      "Bash(rm -rf ~ *)",
      "Bash(sudo *)",
      "Bash(git push --force *)",
      "Bash(git push -f *)",
      "Bash(git reset --hard *)",
      "Bash(chmod 777 *)"
    ]
  },
  "language": "日本語"
}
```

- `allow` の `Bash(*)` ＝すべてのコマンドを許可（deny で除外したものを除く）
- `defaultMode: acceptEdits` ＝ファイルの作成・編集・削除を確認なしで自動承認
- `deny` ＝ルート・ホームの全削除や強制プッシュなど、**取り返しのつかない操作だけ**をブロック

> 設定の確認は、Claude Code 内で `/permissions` を実行します。通常のファイル削除（`rm file.txt`）はブロックされません。

## 付録 C：Claude Code 基本コマンド・モード

| コマンド | 機能 |
|---|---|
| `claude --enable-auto-mode` | 自動承認モードで開始（本研修推奨） |
| `claude` | 通常モードで開始（操作のたびに確認） |
| `claude -c` | 直近の会話を続行 |
| `claude -r` | 過去のセッションを選んで再開 |
| `/init` | プロジェクトを分析して `CLAUDE.md` を生成 |
| `/clear` | 会話履歴をクリア（別の話題へ） |
| `/compact` | 長い会話を要約して精度を保つ |
| `/mcp` | MCP 接続の管理・確認 |
| `/schedule` | 予約実行・定期実行を仕掛ける |
| `/mem-search 〇〇` | 記憶を全プロジェクト横断で検索 |
| `/permissions` | コマンド許可設定の確認 |
| `/help` | コマンド一覧・ヘルプを表示 |
| `/remote-control`（`/rc`） | PC のセッションをスマホ／別ブラウザから操作・確認（有料プラン＋claude.ai ログインが必要） |
| `Esc` | 実行中のアクションを止める |
| `Esc` `Esc` | 巻き戻しメニューを開く |

**モード切替（`Shift + Tab`）**：通常 → 自動承認 → プランモード → 通常…の順に切り替わります。

**コンテキスト管理のコツ**：別タスクに移るときは `/clear`、長くなって精度が落ちたら `/compact`、前回の続きは `claude -c`。

## 付録 D：連鎖 2 スキルの SKILL.md サンプル

研修用フォルダの `.claude/skills/<スキル名>/SKILL.md` として配置します（→ 第 2 回 Part 2-4）。

### `meeting-to-doc/SKILL.md`

```markdown
---
name: meeting-to-doc
description: 営業議事録から提案資料の草案を作り、Google Drive に保存し、保存先 URL を Linear チケットに追記する。議事録・打ち合わせメモから資料を起こすときに使う。
---

## When to use this skill（いつ使うか）
- 議事録（.md）や Linear チケットを起点に、提案資料の草案を作りたいとき

## How to use this skill（手順）
1. 対象の議事録を読み込む（ファイル指定、または Linear チケットに紐づく議事録）
2. 提案資料の草案を Markdown で作成する。構成は次の順：
   - 先方の課題整理（箇条書き）
   - 提案の方針（3点程度）
   - 想定スケジュール
   - 概算費用のレンジ（議事録から読み取れる範囲で）
3. 作成した草案を Google Drive の指定フォルダに保存する
   （ファイル名は「<案件名>_提案資料草案」）
4. 保存先の URL を、対象の Linear チケットの説明（またはコメント）に追記する
5. 完了したら「資料の草案ができました。次に会議調整（meeting-scheduler）を
   実行しますか？」とユーザーに確認する

## 注意
- 数字（費用・規模）は議事録に根拠がある範囲だけ書く。推測は「要確認」と明記する
- 送信や共有設定の変更は行わない（保存と URL 追記まで）
```

### `meeting-scheduler/SKILL.md`

```markdown
---
name: meeting-scheduler
description: 自社メンバーの Google Calendar の空きから会議候補日を3つ選んで仮予定として登録し、会議案内メールの下書きを Gmail に作成して、Linear のステータスを更新する。提案会議や打ち合わせを設定したいときに使う。
---

## When to use this skill（いつ使うか）
- 提案会議・打ち合わせの日程調整と、先方への案内メール下書きをまとめて用意したいとき

## How to use this skill（手順）
1. 対象の Linear チケットと、参加させたい自社メンバーを確認する
2. Google Calendar で自社メンバーの予定を確認し、指定期間（既定は来週の平日）で
   全員が空いている候補日を3つ選ぶ
3. 選んだ3つを「仮」予定としてカレンダーに登録する（タイトル先頭に【仮】）
4. 会議案内メールの下書きを Gmail に作成する
   - 候補日3つを提示し、先方に都合を伺う文面
   - 件名・宛先・本文を整える。**送信はしない（下書きのみ）**
5. Linear チケットのステータスを Todo → In Progress に更新し、
   候補日とメール下書きの状況をコメントで残す
6. 完了したら「下書きを確認して送信してください」とユーザーに伝える

## 注意
- メールは必ず「下書き」まで。送信は人が確認してから行う
- 候補日が3つ揃わない場合は、揃わない旨と空き状況を報告する
```

> これらはサンプルです。会社のメンバー名・Drive フォルダ・既定期間などは、自社に合わせて書き換えてください。

## 付録 E：MCP 接続先・トラブルシュート

**接続先 URL**

| MCP | 接続先 |
|---|---|
| Google Drive / Gmail / Google Calendar | 講師から共有された URL |
| Linear | `https://mcp.linear.app/sse` |

**ターミナルから追加する場合（上級者向け）**

```
claude mcp add --transport sse linear https://mcp.linear.app/sse
claude mcp list        # 一覧確認
claude mcp remove linear   # 削除
```

**よくあるつまずき**

| 症状 | 対処 |
|---|---|
| `needs auth` で止まる | `/mcp` でその行を選び「Authenticate」を再実行。ワークスペース選択ミスなら一度 remove して再追加 |
| OAuth で個人アカウントを選んでしまった | やり直して**会社の Google Workspace アカウント**を選ぶ |
| ブラウザが開かない | ポップアップブロックを解除して再試行 |
| `claude: command not found` | `claude doctor` で診断。PATH 未設定なら 2-4 の手順 |
| 「権限がない」（Linear） | ワークスペースで**メンバー権限以上**が必要。管理者に確認 |

## 付録 F：claude-mem の技術詳細（深掘りしたい人向け）

claude-mem の内部構造（保存先 SQLite／ベクトル DB、フック構成、プロジェクト識別ロジック、横断検索の経路、設定項目など）は `claude-mem-deep-dive-reference.md` にまとめています。非エンジニアの方は読まなくて問題ありません。

## 付録 G：受講者が当日までに用意するもの

**第 1 回まで**

- [ ] Mac（管理者権限あり）／インターネット接続
- [ ] 会社の Google Workspace アカウントでログインできる状態
- [ ] すでに Claude の有料プラン（Pro / Max / Team など）に加入済みで、claude.ai にログインできる状態（Claude Code は有料プラン専用）

**第 2 回まで**

- [ ] 第 1 回の内容（Claude Code 起動・記憶）を体験済み
- [ ] Linear アカウント（<https://linear.app> で無料サインアップ。会社で使っていればそのワークスペース。最初の確認は**自分にアサインされたチケットだけ**を対象に）
- [ ] テスト用に、予定がいくつか入った Google Calendar／数件ファイルのある Google Drive フォルダ
- [ ] （任意）Slack を使う場合は、議事録投稿用のチャンネル

## 付録 H：サンプル CLAUDE.md（「勝手に決めない AI」にする設定）

`CLAUDE.md` は、プロジェクトのフォルダに置いておくと **Claude Code が毎回セッション開始時に読み込む設定ファイル**です（→ 2-5 の `/init` で雛形を自動生成できます）。ここに「どう振る舞ってほしいか」を書いておくと、毎回指示しなくても同じ方針で動いてくれます。

下のサンプルは、AI 研究者 **Andrej Karpathy**（※Claude の作者ではなく、AI に作業を任せるときの「落とし穴」を整理したことで知られる研究者）が公開している指針（[multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills)）を、非エンジニアの業務向けにかみ砕いたものです。**「思い込みで進めず、まず確認する」** を中心に据えています（→ 2-9）。

![Andrej Karpathy skills](https://opengraph.githubassets.com/1/multica-ai/andrej-karpathy-skills)
*出典: [GitHub — multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills)*

研修用フォルダ（`~/Desktop/claude-training`）の直下に `CLAUDE.md` という名前で保存してください。

```markdown
# 基本ルール
- 日本語で応答する。専門用語には簡単な補足を添える
- 変更を実行する前に、何をするかを日本語で説明する

# 1. 作る前に考える（勝手に決めない）
- 指示が曖昧なときは、思い込みで進めず、まず質問して確認する
- 解釈が複数ありうるときは、選択肢を並べて確認を取る（黙って1つに決めない）
- 分からないこと・前提が不確かなことは隠さず、先に伝える

# 2. シンプルに作る
- 依頼されたことだけを、最小限の形で実現する
- 頼まれていない機能・装飾・将来用の作り込みを勝手に足さない

# 3. 必要な所だけ直す
- 既存ファイルを直すときは、必要な箇所だけに留める
- 関係ない部分を勝手に書き換えない。既存の体裁・トーンに合わせる

# 4. ゴールを決めてから動く
- 着手前に「何ができれば完成か（成功の条件）」を一言で定義する
- 複数ステップの作業は、先に短い計画を出し、各段階で確認できるようにする
- 大きな作業はプランモードで計画を見せてから実行する

# ファイル・成果物
- ファイル名は半角英数字とハイフン（例: sales-report.md）
- 成果物はこのフォルダの中に保存する（業務の本番フォルダに混ぜない）

# 安全性
- パスワード・APIキーなどの機密情報をファイルに書き込まない
- 取り返しのつかない操作（広い範囲の削除など）は実行前に必ず確認する
```

> **使い方**：このファイルを置いてから Claude Code を起動すると、起動時に自動で読み込まれます。とくに「1. 作る前に考える」を入れておくと、荒い指示を投げても **勝手に突っ走らず、要件をヒアリングしてくれる**ようになります（→ 2-9）。自社のルール（呼称・トーン・保存先など）に合わせて自由に書き換えてください。

## 学習リソース

| リソース | URL |
|---|---|
| 公式ドキュメント（日本語） | <https://code.claude.com/docs/ja/overview> |
| クイックスタート | <https://code.claude.com/docs/ja/quickstart> |
| MCP ドキュメント（日本語） | <https://code.claude.com/docs/ja/mcp> |
| MCP 公式 | <https://modelcontextprotocol.io> |
| claude-mem 公式リポジトリ | <https://github.com/thedotmack/claude-mem> |
