# Claude に「記憶」を持たせよう — claude-mem 研修資料

> 対象：非エンジニアの方
> 所要時間：30 分（インストール 5 分＋解説 15 分＋実演 10 分）
> 詳細な技術解説は `claude-mem-deep-dive-reference.md` を参照

---

## 1. claude-mem って何？

**ひとことで言うと：Claude Code に「日記帳」を持たせるプラグインです。**

通常の Claude Code は、セッション（会話）を閉じると内容を忘れてしまいます。
claude-mem を入れると、次のような体験ができます：

- 昨日の続きから話せる
- 「前に解決した問題」を思い出してくれる
- プロジェクトごとに記憶が整理される

イメージとしては「Claude にメモ帳と付箋を渡してあげる」感覚です。

---

## 2. たとえ話：Claude の引き出し

```
┌──────────────────────────────────────┐
│  あなたのパソコン                       │
│                                       │
│   📓 Claude 専用の日記帳（1冊だけ）     │
│  ┌────────────────────────────┐     │
│  │ 🏷️ source-code-review のページ │     │
│  │ 🏷️ cbox-coaching のページ      │     │
│  │ 🏷️ copy_site のページ          │     │
│  │ 🏷️ ...                         │     │
│  └────────────────────────────┘     │
└──────────────────────────────────────┘
```

- 📓 **日記帳はパソコンに 1 冊だけ**（場所：`~/.claude-mem/`）
- 🏷️ **付箋にはフォルダ名のラベル**が付く
- 🤖 **Claude が応答するたび**に、裏で別の小さな Claude が要約を書き込む
- 👀 **新しい会話を始めると**、同じラベルの付箋を自動で眺めて思い出す

---

## 3. インストール手順（5 分）

### ステップ 1：Claude Code を開く

ターミナルで `claude` と入力してセッションを開始します。

### ステップ 2：マーケットプレイスを登録

Claude Code の入力欄に以下をそのまま打ち込んでください：

```
/plugin marketplace add thedotmack/claude-mem
```

### ステップ 3：プラグインをインストール

```
/plugin install claude-mem
```

### ステップ 4：Claude Code を再起動

ターミナルで一度 `exit` してから、再度 `claude` を立ち上げます。

これだけで完了です。次回からは何もしなくても、新しいセッションを開くと冒頭に過去の記憶（`$CMEM ...` という表示）が自動で出てきます。

> ⚠️ **注意**：`npm install -g claude-mem` という別のコマンドもありますが、こちらはライブラリのみで**プラグインとしては動きません**。必ず上記の `/plugin` コマンドを使ってください。

---

## 4. 動作確認

### 確認① 冒頭に記憶が出るか

新しいセッションを開いたとき、画面上部に下のような表示が出ていれば成功です：

```
# $CMEM your-folder-name 2026-xx-xx ...
Legend: 🎯session 🔴bugfix 🟣feature ...
ID xxxx ...
```

### 確認② Web UI を開く

ブラウザで以下を開くと、リアルタイムで蓄積される記憶を見られます：

```
http://localhost:37777
```

---

## 5. 知っておくべきルール（重要）

### ルール①：記憶の仕分けは「フォルダ名の末尾」だけ

Claude は「いま開いているフォルダ名」だけを見て記憶を仕分けています。

| あなたが今いるフォルダ | Claude が見るラベル |
|---|---|
| `/Users/me/work/project-a` | `project-a` |
| `/Users/me/private/project-a` | `project-a` ← **混ざる！** |
| `/Users/me/work/project-b` | `project-b` |

➡ **同じ名前のフォルダを別の場所に作ると、記憶が混ざります。**

### ルール②：別フォルダの記憶は「自動では」呼ばれない

- 新しいセッションを開いたとき → **今のフォルダのラベルだけ**自動で読まれる
- 他のフォルダの記憶は、**頼めば**思い出してくれる（→ ルール③）

### ルール③：横断検索は明示的にお願いする

```
/mem-search 〇〇の解決方法
```

このように `/mem-search` を使うと、全プロジェクトを横断して検索してくれます。
「前に似たエラーをどう直したっけ？」と聞くと、Claude が自動で横断検索を呼んでくれることもあります。

---

## 6. 横断的に使える条件・使えない条件まとめ

| シチュエーション | 過去の記憶が使えるか |
|---|---|
| **同じフォルダ**で再起動 | ⭕ 自動で呼ばれる |
| **別フォルダ**で新セッション | ❌ 自動では呼ばれない（明示検索なら ⭕） |
| **同名の別フォルダ**で起動 | ⚠️ 混ざってしまう（要注意） |
| 「前に〇〇したよね？」と質問 | ⭕ Claude が必要に応じて横断検索 |
| `/mem-search` を直接実行 | ⭕ 全プロジェクト検索 |
| フォルダをリネームした後 | ❌ 旧名の記憶は自動では出ない |

---

## 7. ハンズオン（10 分）

### 演習 1：記憶を書き込む

1. ターミナルで適当なフォルダに移動：
   ```
   cd ~/Documents/test-folder
   ```
2. Claude を起動：
   ```
   claude
   ```
3. 何か質問する：
   ```
   このフォルダで HTML の研修資料を作りたいです。タイトル候補を 3 つ教えて。
   ```
4. Claude の応答後、`exit` で終了する。

### 演習 2：記憶が呼ばれるか確認する

1. 同じフォルダでもう一度 `claude` を起動。
2. 冒頭に `$CMEM test-folder ...` という表示があれば成功。
3. 質問してみる：
   ```
   前回どんなタイトル候補を出してくれましたか？
   ```
   → 以前の内容を覚えていれば成功です。

### 演習 3：別フォルダでは引き継がれないことを確認

1. 別のフォルダに移動：
   ```
   cd ~/Documents/another-folder
   claude
   ```
2. 同じ質問をする：
   ```
   前回どんなタイトル候補を出してくれましたか？
   ```
   → 「わかりません」と答えるはず（フォルダ名が違うため）。

### 演習 4：横断検索を試す

```
/mem-search タイトル候補
```

→ 別フォルダの内容も含めて検索されることを確認。

---

## 8. よくある質問

### Q1. 記憶はどこに保存されますか？

`~/.claude-mem/` というホームフォルダ直下の隠しフォルダの中です。
**作業しているプロジェクトフォルダの中ではありません**ので、git にコミットされる心配はありません。

### Q2. 機密情報も保存されますか？

会話の内容はすべて要約されて保存されます。
**機密情報や個人情報を扱うフォルダでは、claude-mem を無効化する**か、扱う情報に注意してください。
無効化したいプロジェクトは `~/.claude-mem/settings.json` の `CLAUDE_MEM_EXCLUDED_PROJECTS` に追加できます。

### Q3. ストレージ容量はどれくらい必要？

- 通常使用で 100MB 〜 数 GB 程度
- 1 年使い込むと 1〜2GB 程度が目安
- 中身の確認：`du -sh ~/.claude-mem`

### Q4. 記憶を全部消したい

```bash
rm -rf ~/.claude-mem
```

を実行すると全削除されます（**復元不可**なので慎重に）。

### Q5. プロジェクトのフォルダ名を変えたら過去の記憶はどうなる？

旧名で残ったままになります。エンジニアに依頼すれば DB 内のラベルを書き換えて移行できます。

### Q6. 他のパソコンと記憶を共有できる？

標準では共有されません。`~/.claude-mem/` フォルダを Dropbox などで同期すれば共有可能ですが、競合に注意が必要です。

---

## 9. 運用のコツ

✅ **やったほうがいいこと**
- プロジェクトごとにフォルダ名をユニークにする
- 大事な記憶は `~/.claude-mem/` を定期的にバックアップ
- フォルダをリネームしたらエンジニアにラベル移行を相談

⚠️ **避けたほうがいいこと**
- 同名のフォルダを別の場所に複数作る
- ホームディレクトリ直下 (`cd ~`) から `claude` を起動する（記憶が「unknown-project」に集約されてごちゃ混ぜになる）
- 機密情報を扱うフォルダで無設定のまま使う

---

## 10. まとめ

| ポイント | 一言でいうと |
|---|---|
| 何をするツールか | Claude Code に記憶を持たせる |
| 保存場所 | `~/.claude-mem/`（パソコン内、ユーザー共通） |
| 仕分けの単位 | 「いま開いているフォルダの名前」 |
| 自動で思い出すか | 同じフォルダ名なら ⭕／違えば ❌ |
| 他フォルダの記憶 | `/mem-search` で横断検索できる |
| インストール | `/plugin marketplace add thedotmack/claude-mem` → `/plugin install claude-mem` → 再起動 |

> 「Claude が前回の話を覚えていてくれる」便利さは、一度体験すると元には戻れません。
> ぜひ自分の作業フォルダで試してみてください。

---

## 11. もう一つの記憶機能：Claude Code 標準メモリ

実は Claude Code には **claude-mem とは別に、もう一つの「記憶」機能が最初から組み込まれて**います。
インストール不要で、すでに動いています。

### 11.1 どこにあるか

```
~/.claude/projects/<エンコードされたフォルダパス>/memory/
├── MEMORY.md                ← 1行ずつのインデックス（目次）
└── 各記憶のmdファイル        ← 1ファイル＝1つの覚えてほしい事実
```

このプロジェクト（`source-code-review`）でも、実際に下のファイルが残っています：

```
~/.claude/projects/-Users-khayashida-development-client-works-ibm-source-code-review/memory/
├── MEMORY.md
└── tobe-method2-result.md   ← TOBE規模推計「方法2」の最終結果が記録済み
```

### 11.2 claude-mem との違い（一言で）

| 機能 | ひとことで言うと |
|---|---|
| **① claude-mem** | 会話のたびに**裏で勝手にメモを取る秘書** |
| **② 標準メモリ** | 私（Claude）が「これは大事」と判断したら**意図的に書き留める手帳** |

### 11.3 仕組みの違い

| 観点 | ① claude-mem | ② 標準メモリ |
|---|---|---|
| インストール | 必要（`/plugin install`） | 不要（標準機能） |
| 保存場所 | `~/.claude-mem/` の SQLite DB | `~/.claude/projects/.../memory/` のmdファイル |
| 仕分け | フォルダ名の末尾だけ | フォルダのフルパス（衝突しない） |
| 書く人 | 裏で動く小さな Claude（自動） | 会話中の Claude が必要だと判断したとき |
| 書くタイミング | 応答が終わるたび毎回 | 重要な確定事項が出たときだけ |
| 書く内容 | ツール実行の要約・作業ログ | 結論・判断・確定事実だけ厳選 |
| 量 | 多い（細かい記録が大量に） | 少ない（厳選） |
| 自動で思い出す | 同じフォルダ名で新セッション開始時 | 毎回 `MEMORY.md` の目次が自動で読まれる |
| 形式 | DB（中身は見にくい） | プレーンな Markdown（人間が読める） |
| 横断検索 | `/mem-search` で全プロジェクト検索可 | プロジェクト単位（明示指定すれば読める） |

### 11.4 標準メモリの使い方

特別なコマンドは不要です。Claude との会話の中で次のように頼むだけ：

```
これは重要なので覚えておいてください：
TOBE規模推計の最終値は 903,904 step です。
```

すると Claude が判断して `~/.claude/projects/.../memory/` 配下に Markdown ファイルを作り、
`MEMORY.md`（目次）にも 1 行追記してくれます。

次回以降のセッションでは、目次が自動的に読み込まれるため、関係しそうなときに Claude が個別ファイルを読み返します。

### 11.5 なぜこの 2 つで十分か（独自 hook は不要）

「自分でプロジェクトフォルダ内に要約を書き込む仕組みを作ったほうがいいのでは？」と思うかもしれません。
結論から言うと **作らないほうがいい** です。理由は 3 つあります。

**理由①：役割分担がすでに完成している**

```
┌──────────────────────────────────────────┐
│ ① claude-mem（自動・大量）                  │
│  → 「何をやったか」の作業ログ的記録         │
│  → 新セッション開始時に過去 50 件が自動で蘇る  │
│  → 横断検索したいとき /mem-search           │
└──────────────────────────────────────────┘
                    ＋
┌──────────────────────────────────────────┐
│ ② 標準メモリ（半手動・厳選）                 │
│  → 「結論・確定事項・判断」など重要事実だけ   │
│  → 私が「これは残すべき」と判断したものだけ   │
│  → 例：最終推計値、設計判断、確定仕様         │
└──────────────────────────────────────────┘
```

すでにこの 2 階建てで「細かい記録」と「重要な結論」がきれいに分業されています。

**理由②：プロジェクトフォルダ内に書くと "AI 痕跡" が残る**

業務指示の中には「成果物に AI / agent の形跡を残さない」というルールがある案件が多くあります。
プロジェクトフォルダ内に `.claude-memory/` のようなフォルダを作ってしまうと、
**顧客納品物に AI の存在が紛れ込むリスク**が出てきます。

①と②はどちらも**プロジェクトの外**に保存されるので、この問題が発生しません。

**理由③：メンテナンスコストがかかる**

独自に hook を組むと、壊れたときに自分で直さないといけません。
①と②は公式が面倒を見てくれます。

### 11.6 まとめ：2 階建てを活用しよう

| やりたいこと | 使う仕組み | やり方 |
|---|---|---|
| 「昨日の続き」を自然に思い出してほしい | ① claude-mem | 何もしなくていい（自動） |
| 別プロジェクトの似た事例を探す | ① claude-mem | `/mem-search 〇〇` |
| 重要な数字・結論を確実に覚えてほしい | ② 標準メモリ | 「これを覚えてください：◯◯」と頼む |
| プロジェクト固有の決定を蓄積 | ② 標準メモリ | 上と同じ |
| プロジェクトフォルダ内に書き込む | **使わない** | AI 痕跡が残るため非推奨 |

> 💡 **覚えるコツ**
> 「秘書」（claude-mem）が毎日の作業ログを自動で残してくれ、
> 「自分の手帳」（標準メモリ）に大事な結論だけ書き留める。
> この 2 つを使い分けるイメージです。

---

## 参考リンク

- 公式リポジトリ：<https://github.com/thedotmack/claude-mem>
- Web UI（ローカル）：<http://localhost:37777>
- 詳細な技術解説：`claude-mem-deep-dive-reference.md`
